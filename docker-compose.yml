services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: herostack-app
    restart: unless-stopped
    ports:
      - "3056:3056"
    environment:
      - DB_HOST=${DB_HOST:-postgres}
      - DB_PORT=${DB_PORT:-5432}
      - DB_USER=${DB_USER:-herostack}
      - DB_PASSWORD=${DB_PASSWORD:-herostack123}
      - DB_NAME=${DB_NAME:-herostack}
      - AUTH_SECRET=${AUTH_SECRET}
      - AUTH_URL=${AUTH_URL:-http://localhost:3056}
      - AUTH_GOOGLE_ID=${AUTH_GOOGLE_ID:-}
      - AUTH_GOOGLE_SECRET=${AUTH_GOOGLE_SECRET:-}
      - AUTH_GITHUB_ID=${AUTH_GITHUB_ID:-}
      - AUTH_GITHUB_SECRET=${AUTH_GITHUB_SECRET:-}
    volumes:
      - uploads_data:/app/public/uploads
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3056"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  postgres:
    image: postgres:16-alpine
    container_name: herostack-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-herostack}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-herostack123}
      POSTGRES_DB: ${DB_NAME:-herostack}
    ports:
      - "5432:5432"
    volumes:
      - herostack_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U herostack -d herostack"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  herostack_data:
  uploads_data:
